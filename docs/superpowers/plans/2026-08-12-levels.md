# 레벨 체계 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 토큰 수를 그대로 쓰던 경험치를 본가의 경험치·레벨 체계(레벨 1~100, 6가지 성장 곡선, 실제 `min_level` 진화)로 바꾸고, 알을 경험치에서 떼어내 자기 계량기로 옮긴다.

**Architecture:** 성장 곡선은 순수 함수 한 파일(`GrowthRate`)로 두고, 성장 타입은 **개체가 들고 다닌다** — 레벨 계산이 네트워크 없이 항상 가능해야 하기 때문이다(사용량 갱신 경로에는 `EvoLine` 이 없다). 진화 조건은 PokéAPI 파싱을 넓혀 `min_level`·`held_item` 을 읽고, PokéAPI 가 레벨을 안 주는 22종만 번들 카탈로그로 채운다. 알은 `Individual.eggProgress` 라는 별도 필드가 되어 경험치와 서로를 깎지 않는다.

**Tech Stack:** Swift 6 / SwiftPM / SwiftUI / macOS 14 / 외부 의존성 없음

**설계 문서:** `docs/superpowers/specs/2026-08-12-levels-design.md` — 수치의 근거(실측 분포·31종 전수 조사)가 거기 있다. 이 계획은 그 값을 그대로 옮긴다.

## Global Constraints

- 커밋 메시지·PR 은 **영어**, 코드 주석은 **한국어** (CLAUDE.md 기여 언어 규약).
- 사용자 문구는 ko/en/ja 셋 다 — `L.t(ko, en, ja)`.
- 빌드 경고 0. 검증은 **`swift build --build-tests`** 로 한다(`swift build` 는 테스트 타깃을 안 짓는다).
- 외부 의존성 추가 금지.
- 실제 세이브(`~/Library/Application Support/PokeDexBar/player-state.json`)를 읽거나 쓰지 않는다. 테스트는 임시 파일 + `#if DEBUG` 헬퍼(`seedForTesting`/`addForTesting`)를 쓴다.
- **관대 디코딩의 짝은 값 범위 검증이다** — 새 수치 필드는 경계 한 곳에서 자른다.
- 순수 판정은 부수효과와 분리해 순수 함수로 테스트한다.
- 테스트는 **결함 조건을 실제로 밟아야** 한다. 단언 양쪽이 같은 입력이면 그 테스트는 아무것도 안 지킨다.

---

### Task 1: 성장 곡선

**Files:**
- Create: `Sources/PokeDexBar/Player/GrowthRate.swift`
- Test: `Tests/PokeDexBarTests/GrowthRateTests.swift`

**Interfaces:**
- Consumes: 없음(순수)
- Produces:
  - `enum GrowthRate: String, Codable, Sendable, CaseIterable { case erratic, fast, mediumFast, mediumSlow, slow, fluctuating }`
  - `static func fromAPI(_ name: String) -> GrowthRate`
  - `func totalExp(at level: Int) -> Int`
  - `func level(forExp exp: Int) -> Int`
  - `static let maxLevel = 100`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```swift
import XCTest
@testable import PokeDexBar

final class GrowthRateTests: XCTestCase {
    /// 100레벨 도달치 — 본가 표 그대로. 이 여섯이 맞으면 곡선 전체가 맞다고 본다.
    func testTotalExpAtOneHundredMatchesTheGames() {
        XCTAssertEqual(GrowthRate.erratic.totalExp(at: 100), 600_000)
        XCTAssertEqual(GrowthRate.fast.totalExp(at: 100), 800_000)
        XCTAssertEqual(GrowthRate.mediumFast.totalExp(at: 100), 1_000_000)
        XCTAssertEqual(GrowthRate.mediumSlow.totalExp(at: 100), 1_059_860)
        XCTAssertEqual(GrowthRate.slow.totalExp(at: 100), 1_250_000)
        XCTAssertEqual(GrowthRate.fluctuating.totalExp(at: 100), 1_640_000)
    }

    /// 중간 지점도 본가 표와 맞는지 — 100레벨만 맞고 중간이 틀린 공식이 있을 수 있다.
    func testMidCurveValuesMatchTheGames() {
        XCTAssertEqual(GrowthRate.mediumSlow.totalExp(at: 16), 2_535)
        XCTAssertEqual(GrowthRate.mediumSlow.totalExp(at: 36), 40_007)
        XCTAssertEqual(GrowthRate.mediumFast.totalExp(at: 16), 4_096)
        XCTAssertEqual(GrowthRate.slow.totalExp(at: 16), 5_120)
        XCTAssertEqual(GrowthRate.erratic.totalExp(at: 16), 6_881)
        XCTAssertEqual(GrowthRate.fluctuating.totalExp(at: 16), 2_457)
    }

    /// **1레벨은 언제나 0이다.** `mediumSlow` 공식은 n=1 에서 −53.8 을 낸다 —
    /// 특수 처리를 안 하면 음수 경험치가 생겨 레벨 계산이 통째로 깨진다.
    func testLevelOneIsAlwaysZeroEvenWhereTheFormulaGoesNegative() {
        for rate in GrowthRate.allCases {
            XCTAssertEqual(rate.totalExp(at: 1), 0, "\(rate)")
            XCTAssertEqual(rate.totalExp(at: 0), 0, "\(rate)")
            XCTAssertEqual(rate.totalExp(at: -5), 0, "\(rate)")
        }
    }

    /// 곡선은 단조 증가한다 — 어느 구간에서든 레벨이 오르면 필요 경험치도 오른다.
    /// (`erratic`·`fluctuating` 은 구간이 나뉘어 있어 이음매에서 역전하기 쉽다.)
    func testCurvesNeverGoBackwards() {
        for rate in GrowthRate.allCases {
            for level in 2...GrowthRate.maxLevel {
                XCTAssertGreaterThan(rate.totalExp(at: level), rate.totalExp(at: level - 1),
                                     "\(rate) L\(level)")
            }
        }
    }

    /// `level(forExp:)` 은 `totalExp(at:)` 의 역이다 — 정확히 그 값에서 그 레벨이 된다.
    func testLevelIsTheInverseOfTotalExp() {
        for rate in GrowthRate.allCases {
            for level in 1...GrowthRate.maxLevel {
                let exp = rate.totalExp(at: level)
                XCTAssertEqual(rate.level(forExp: exp), level, "\(rate) L\(level)")
                if level > 1 {
                    XCTAssertEqual(rate.level(forExp: exp - 1), level - 1,
                                   "\(rate) L\(level) 직전 1EXP")
                }
            }
        }
    }

    /// 100을 넘는 경험치가 들어와도 레벨은 100에서 멈춘다.
    func testLevelStopsAtOneHundred() {
        XCTAssertEqual(GrowthRate.mediumFast.level(forExp: 999_999_999), 100)
        XCTAssertEqual(GrowthRate.mediumFast.level(forExp: 0), 1)
        XCTAssertEqual(GrowthRate.mediumFast.level(forExp: -1), 1)
    }

    /// PokéAPI 문자열 매핑. 여섯 개가 전부이고, 모르는 값은 `mediumFast` 로 떨어진다.
    func testAPINamesMapToEveryCase() {
        XCTAssertEqual(GrowthRate.fromAPI("slow-then-very-fast"), .erratic)
        XCTAssertEqual(GrowthRate.fromAPI("fast"), .fast)
        XCTAssertEqual(GrowthRate.fromAPI("medium"), .mediumFast)
        XCTAssertEqual(GrowthRate.fromAPI("medium-slow"), .mediumSlow)
        XCTAssertEqual(GrowthRate.fromAPI("slow"), .slow)
        XCTAssertEqual(GrowthRate.fromAPI("fast-then-very-slow"), .fluctuating)
        XCTAssertEqual(GrowthRate.fromAPI("나중에 생긴 무언가"), .mediumFast)
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `swift test --filter GrowthRateTests`
Expected: 컴파일 실패 — `cannot find 'GrowthRate' in scope`

- [ ] **Step 3: 구현한다**

```swift
import Foundation

/// 본가의 경험치 성장 곡선 여섯 가지. **순수 표**라 테스트로 잠근다.
///
/// 앱의 `Individual` 이 이 값을 들고 다닌다(라인이 아니라) — 레벨 계산은 사용량 갱신 경로에서
/// 매번 일어나는데 그 자리에는 `EvoLine`(네트워크)이 없기 때문이다. 성격·등급과 같은 취급이다.
enum GrowthRate: String, Codable, Sendable, CaseIterable {
    case erratic, fast, mediumFast, mediumSlow, slow, fluctuating

    static let maxLevel = 100

    /// PokéAPI `pokemon-species.growth_rate.name` → 이 열거형.
    /// PokéAPI 의 growth-rate 리소스가 여섯 개라 이 표가 전부다. 모르는 값은 가장 흔한
    /// `mediumFast` 로 떨어뜨린다 — 데이터가 낯설다고 화면이 멈추면 안 된다.
    static func fromAPI(_ name: String) -> GrowthRate {
        switch name {
        case "slow-then-very-fast": .erratic
        case "fast": .fast
        case "medium": .mediumFast
        case "medium-slow": .mediumSlow
        case "slow": .slow
        case "fast-then-very-slow": .fluctuating
        default: .mediumFast
        }
    }

    /// 그 레벨에 도달하는 데 필요한 **누적** 경험치. 본가 공식 그대로이며 정수 내림이다.
    ///
    /// `level <= 1` 을 0으로 잘라내는 것이 중요하다 — `mediumSlow` 공식은 n=1 에서 −53.8 을
    /// 내고, 그 음수가 그대로 나가면 `level(forExp:)` 이 통째로 어긋난다.
    func totalExp(at level: Int) -> Int {
        guard level > 1 else { return 0 }
        let n = min(level, Self.maxLevel)
        let cube = n * n * n
        switch self {
        case .fast: return 4 * cube / 5
        case .mediumFast: return cube
        case .slow: return 5 * cube / 4
        case .mediumSlow:
            return Int(1.2 * Double(cube)) - 15 * n * n + 100 * n - 140
        case .erratic:
            if n < 50 { return cube * (100 - n) / 50 }
            if n < 68 { return cube * (150 - n) / 100 }
            if n < 98 { return cube * ((1911 - 10 * n) / 3) / 500 }
            return cube * (160 - n) / 100
        case .fluctuating:
            if n < 15 { return cube * ((n + 1) / 3 + 24) / 50 }
            if n < 36 { return cube * (n + 14) / 50 }
            return cube * (n / 2 + 32) / 50
        }
    }

    /// 누적 경험치 → 레벨. `totalExp(at:)` 의 역이며 100에서 멈춘다.
    /// 100번 도는 선형 탐색이라 이분 탐색을 쓸 이유가 없다(호출 빈도가 화면 갱신 수준이다).
    func level(forExp exp: Int) -> Int {
        guard exp > 0 else { return 1 }
        var result = 1
        for level in 2...Self.maxLevel where totalExp(at: level) <= exp { result = level }
        return result
    }
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `swift test --filter GrowthRateTests`
Expected: PASS (7개)

- [ ] **Step 5: 커밋**

```bash
git add Sources/PokeDexBar/Player/GrowthRate.swift Tests/PokeDexBarTests/GrowthRateTests.swift
git commit -m "feat: add the six real experience curves"
```

---

### Task 2: 개체가 성장 타입과 알 진행분을 갖는다

**Files:**
- Modify: `Sources/PokeDexBar/Player/Individual.swift` (프로퍼티·`init`·`init(from:)`·`sanitized()`)
- Modify: `Sources/PokeDexBar/Player/ExpBalance.swift` (환율 상수 둘 — 이전 로직이 지금 쓴다)
- Test: `Tests/PokeDexBarTests/LevelMigrationTests.swift`

**Interfaces:**
- Consumes: `GrowthRate`, `GrowthRate.maxLevel`, `totalExp(at:)`, `level(forExp:)`
- Produces:
  - `Individual.growthRate: GrowthRate`
  - `Individual.eggProgress: Int`
  - `Individual.level: Int` (계산 프로퍼티 = `growthRate.level(forExp: exp)`)
  - `init(...)` 에 `growthRate: GrowthRate = .mediumFast, eggProgress: Int = 0` 인자 추가(기존 인자 뒤, 기본값 있음)
  - `ExpBalance.tokensPerExp = 500`, `ExpBalance.candyExp = 200_000` — 이전 로직이 환율을 쓰므로 여기서 정의한다. 쓰는 곳은 Task 3 이다.

**배경 — 이 파일에서 절대 깨면 안 되는 것:** `Individual` 은 손으로 쓴 `init(from:)` 을 갖고 있고, 그 이유가 파일 주석에 있다. Swift 합성 디코더는 기본값이 있어도 키가 없으면 던지고, `LossyIndividual` 이 그 예외를 "이 개체를 버린다"로 바꾼다 — 필드 하나를 더하면 **기존 세이브의 모든 개체가 조용히 사라진다**. 새 필드는 반드시 `value(.key, 기본값)` 형태로 읽는다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```swift
import XCTest
@testable import PokeDexBar

/// 레벨 이전 세이브가 올라올 때 무엇이 살아남나.
final class LevelMigrationTests: XCTestCase {
    private let now = Date(timeIntervalSince1970: 1_700_000_000)

    private func decode(_ json: String) throws -> Individual {
        try JSONDecoder().decode(Individual.self, from: Data(json.utf8))
    }

    /// **구 세이브에는 두 필드가 없다.** 없다고 개체를 버리면 박스가 통째로 사라진다.
    func testAnOldIndividualStillDecodes() throws {
        let json = """
        {"id":"\(UUID().uuidString)","baseID":4,"speciesID":4,"pathIDs":[4],
         "nature":"hardy","obtainedAt":0,"grade":"common","exp":250000000}
        """
        let individual = try decode(json)
        XCTAssertEqual(individual.speciesID, 4)
        XCTAssertEqual(individual.growthRate, .mediumFast, "성장 타입 기본값이 안 들어갔다")
    }

    /// **알 진행분은 안 뺏는다.** 구 세이브의 `exp` 는 알 계량기이기도 했다.
    func testAnOldSaveKeepsItsEggProgress() throws {
        let json = """
        {"id":"\(UUID().uuidString)","baseID":4,"speciesID":4,"pathIDs":[4],
         "nature":"hardy","obtainedAt":0,"grade":"common","exp":250000000}
        """
        XCTAssertEqual(try decode(json).eggProgress, 250_000_000)
    }

    /// **경험치는 토큰에서 EXP 로 환산된다.** 2.5억 토큰 ÷ 500 = 50만 EXP → mediumFast L79.
    func testAnOldSaveConvertsTokensToExperience() throws {
        let json = """
        {"id":"\(UUID().uuidString)","baseID":4,"speciesID":4,"pathIDs":[4],
         "nature":"hardy","obtainedAt":0,"grade":"common","exp":250000000}
        """
        let individual = try decode(json)
        XCTAssertEqual(individual.exp, 500_000)
        XCTAssertEqual(individual.level, GrowthRate.mediumFast.level(forExp: 500_000))
        XCTAssertEqual(individual.level, 79)
    }

    /// **새 세이브는 환산하지 않는다.** `eggProgress` 키가 있으면 이미 이전이 끝난 것이다 —
    /// 대조군이 없으면 "매번 500으로 나누는" 구현도 위 테스트를 통과한다.
    func testANewSaveIsNotConvertedAgain() throws {
        let json = """
        {"id":"\(UUID().uuidString)","baseID":4,"speciesID":4,"pathIDs":[4],
         "nature":"hardy","obtainedAt":0,"grade":"common","exp":500000,"eggProgress":250000000,
         "growthRate":"mediumSlow"}
        """
        let individual = try decode(json)
        XCTAssertEqual(individual.exp, 500_000, "이미 EXP 인 값을 또 나눴다")
        XCTAssertEqual(individual.eggProgress, 250_000_000)
        XCTAssertEqual(individual.growthRate, .mediumSlow)
    }

    /// 값 범위 검증 — 관대 디코딩의 짝. 산술에 쓰이는 수치라 경계에서 자른다.
    func testAbsurdValuesAreClamped() throws {
        let json = """
        {"id":"\(UUID().uuidString)","baseID":4,"speciesID":4,"pathIDs":[4],
         "nature":"hardy","obtainedAt":0,"grade":"common",
         "exp":9223372036854775807,"eggProgress":9223372036854775807,"growthRate":"slow"}
        """
        let individual = try decode(json).sanitized()
        XCTAssertEqual(individual.exp, GrowthRate.slow.totalExp(at: 100))
        XCTAssertEqual(individual.eggProgress, ExpBalance.eggThreshold(grade: .common))

        let negative = """
        {"id":"\(UUID().uuidString)","baseID":4,"speciesID":4,"pathIDs":[4],
         "nature":"hardy","obtainedAt":0,"grade":"common","exp":-5,"eggProgress":-5}
        """
        let fixed = try decode(negative).sanitized()
        XCTAssertEqual(fixed.exp, 0)
        XCTAssertEqual(fixed.eggProgress, 0)
    }

    /// 레벨은 성장 타입을 따른다 — 같은 경험치라도 타입이 다르면 레벨이 다르다.
    func testLevelFollowsTheGrowthRate() {
        func make(_ rate: GrowthRate) -> Individual {
            Individual(baseID: 1, speciesID: 1, pathIDs: [1], nature: .hardy, exp: 600_000,
                       obtainedAt: now, grade: .common, growthRate: rate)
        }
        XCTAssertEqual(make(.erratic).level, 100)
        XCTAssertEqual(make(.slow).level, 79)
        XCTAssertNotEqual(make(.erratic).level, make(.slow).level)
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `swift test --filter LevelMigrationTests`
Expected: 컴파일 실패 — `value of type 'Individual' has no member 'growthRate'`

- [ ] **Step 3: 구현한다**

`Individual` 에 프로퍼티를 더한다(다른 저장 프로퍼티 옆):

```swift
    /// 이 개체의 경험치 곡선. **개체가 들고 다니는** 이유는 `GrowthRate` 주석에 있다 —
    /// 레벨 계산 자리에 네트워크가 없기 때문이다. 진화하면 새 종의 것으로 갱신한다.
    var growthRate: GrowthRate = .mediumFast
    /// 알 계량기. 파트너인 동안 쓴 **토큰**만큼 찬다(EXP 가 아니다 — 임계가 토큰 단위다).
    /// `exp` 와 서로를 깎지 않는다. 예전에는 `exp` 하나가 두 역할을 겸했고, 그것 때문에
    /// 등급이 경험치에 끼어들어야 했다.
    var eggProgress: Int = 0

    /// 지금 레벨. 경험치에서 파생되므로 따로 저장하지 않는다 — 저장하면 둘이 어긋날 수 있다.
    var level: Int { growthRate.level(forExp: exp) }
```

`init(...)` 시그니처에 인자를 더한다(기존 인자 순서는 그대로, 뒤에 붙인다):

```swift
         grade: Grade, form: String? = nil, region: Region? = nil, regionVariant: String? = nil,
         growthRate: GrowthRate = .mediumFast, eggProgress: Int = 0) {
        ...
        self.growthRate = growthRate
        self.eggProgress = eggProgress
    }
```

`init(from:)` 에 이전 로직을 더한다(다른 `value(...)` 호출들 뒤):

```swift
        growthRate = value(.growthRate, .mediumFast)
        // **레벨 이전 세이브 판별.** `eggProgress` 키가 없으면 `exp` 는 아직 "쓴 토큰" 이다.
        // 그때는 그 값을 둘로 나눠 준다 — 알 진행분은 그대로 물려주고(쌓아 둔 걸 뺏지 않는다),
        // 경험치는 환율로 나눠 실제 EXP 로 만든다. 키가 있으면 이미 이전이 끝난 세이브다.
        if let carried = try? c.decode(Int.self, forKey: .eggProgress) {
            eggProgress = carried
        } else {
            eggProgress = exp
            exp = exp / ExpBalance.tokensPerExp
        }
```

`sanitized()` 에 값 범위 검증을 더한다(기존 폼·지방 정리 뒤, `return` 앞):

```swift
        // 관대 디코딩의 짝 — 산술에 쓰이는 수치는 경계 한 곳에서 자른다(CLAUDE.md).
        // 자르지 않으면 `Int.max` 가 그대로 저장돼 이후 덧셈이 오버플로 트랩으로 프로세스를 죽이고,
        // 재기동해도 같은 파일을 읽어 또 죽는다.
        fixed.exp = min(fixed.growthRate.totalExp(at: GrowthRate.maxLevel), max(0, fixed.exp))
        fixed.eggProgress = min(ExpBalance.eggThreshold(grade: fixed.grade), max(0, fixed.eggProgress))
```

`CodingKeys` 에 `growthRate`, `eggProgress` 를 더한다.

`ExpBalance.swift` 에 상수 둘을 더한다(`eggThreshold` 는 그대로, `threshold(grade:stageIndex:)` 도 아직 지우지 않는다 — 쓰는 곳이 Task 7·8 에서 사라진다):

```swift
    /// 토큰 몇 개가 1EXP 인가. **이 앱의 손잡이**이고, 곡선은 본가 값 그대로다.
    /// 500인 근거: 실측 하루 평균 3.65억 토큰에서 `mediumFast` 만렙(5억 토큰)이 약 1.5일.
    static let tokensPerExp = 500

    /// 경험치 사탕 하나가 주는 EXP. 예전 값(1억 토큰)을 환율로 나눈 값이라 **값어치가 안 바뀐다**.
    static let candyExp = 200_000
```

- [ ] **Step 4: 통과를 확인한다**

Run: `swift test --filter LevelMigrationTests` → PASS (6개)
Run: `swift build --build-tests` → 경고 0

- [ ] **Step 5: 커밋**

```bash
git add Sources/PokeDexBar/Player/Individual.swift Tests/PokeDexBarTests/LevelMigrationTests.swift
git commit -m "feat: give each individual its growth rate and its own Egg meter"
```

---

### Task 3: 환율과 두 계량기

**Files:**
- Modify: `Sources/PokeDexBar/Player/ExpBalance.swift`
- Modify: `Sources/PokeDexBar/Player/PlayerStore.swift` (`update` 안의 파트너 적립 블록)
- Modify: `Sources/PokeDexBar/Player/PlayerStore+Shop.swift` (`expCandyAmount`, `useExpCandy`)
- Modify: `Sources/PokeDexBar/Player/PlayerStore+FoundEgg.swift` (`canTakeFoundEgg`, `takeFoundEgg`)
- Modify: `Sources/PokeDexBar/UI/FoundEggAnnouncementCard.swift:59` (판정을 `eggProgress` 로)
- Test: `Tests/PokeDexBarTests/LevelEconomyTests.swift`

**Interfaces:**
- Consumes: `Individual.exp`, `Individual.eggProgress`, `Individual.level`, `GrowthRate`
- Produces:
  - `ExpBalance.eggThreshold(grade:)` — **값 그대로 유지**
  - `ExpBalance.threshold(grade:stageIndex:)` — **이 태스크에서는 손대지 않는다.** 쓰는 곳이 둘 남아 있고(진화·박사), 각각 Task 7·8 에서 사라진다. 여기서 지우면 세 파일이 동시에 깨져 중간 상태를 만들어야 한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```swift
import XCTest
@testable import PokeDexBar

/// 토큰이 경험치와 알로 나뉘어 들어간다.
@MainActor
final class LevelEconomyTests: XCTestCase {
    private let now = Date(timeIntervalSince1970: 1_700_000_000)

    private func makeStore() -> PlayerStore {
        PlayerStore(fileURL: FileManager.default.temporaryDirectory
                        .appendingPathComponent("lvl-\(UUID().uuidString).json"),
                    rng: SeededRNG(seed: 1), now: { self.now })
    }

    private func partnered(_ store: PlayerStore, rate: GrowthRate = .mediumFast) -> UUID {
        let individual = Individual(baseID: 4, speciesID: 4, pathIDs: [4], nature: .hardy,
                                    obtainedAt: now, grade: .common, growthRate: rate)
        store.addForTesting(individual)
        store.setPartner(individual.id)
        return individual.id
    }

    private func partner(_ store: PlayerStore, _ id: UUID) -> Individual {
        store.state.box.first { $0.id == id }!
    }

    /// **500토큰이 1EXP 다.**
    func testTokensBecomeExperienceAtFiveHundredToOne() {
        let store = makeStore()
        let id = partnered(store)
        store.update(todayTokens: 500_000, todayDate: "2026-08-12", hasUsageData: true)

        XCTAssertEqual(partner(store, id).exp, 1_000, "환율이 안 걸렸다")
    }

    /// **알은 토큰 그대로 찬다.** 경험치와 같은 수가 되면 분리가 안 된 것이다.
    func testTheEggMeterCountsRawTokens() {
        let store = makeStore()
        let id = partnered(store)
        store.update(todayTokens: 500_000, todayDate: "2026-08-12", hasUsageData: true)

        let p = partner(store, id)
        XCTAssertEqual(p.eggProgress, 500_000, "알 계량기가 토큰을 그대로 안 센다")
        XCTAssertNotEqual(p.eggProgress, p.exp, "두 계량기가 같은 값이다 — 분리가 안 됐다")
    }

    /// **파트너가 아니면 둘 다 안 찬다.**
    func testANonPartnerGainsNothing() {
        let store = makeStore()
        let bench = Individual(baseID: 1, speciesID: 1, pathIDs: [1], nature: .hardy,
                               obtainedAt: now, grade: .common)
        store.addForTesting(bench)
        _ = partnered(store)   // 다른 개체가 파트너다
        store.update(todayTokens: 500_000, todayDate: "2026-08-12", hasUsageData: true)

        let idle = store.state.box.first { $0.id == bench.id }!
        XCTAssertEqual(idle.exp, 0)
        XCTAssertEqual(idle.eggProgress, 0)
    }

    /// **최종진화가 아니어도 알이 찬다.** 예전 규칙(최종형만)이 남아 있으면 여기서 걸린다.
    func testAnUnevolvedPartnerFillsTheEggMeter() {
        let store = makeStore()
        let id = partnered(store)   // 파이리 — 최종형이 아니다
        store.update(todayTokens: 1_000_000, todayDate: "2026-08-12", hasUsageData: true)

        XCTAssertGreaterThan(partner(store, id).eggProgress, 0)
    }

    /// **경험치 부적은 경험치에만 걸린다.** 알까지 2배가 되면 분리가 샌 것이다.
    func testTheExpCharmDoublesExperienceButNotTheEgg() {
        let store = makeStore()
        let id = partnered(store)
        store.mutate { $0.ownsExpCharm = true }
        store.update(todayTokens: 500_000, todayDate: "2026-08-12", hasUsageData: true)

        let p = partner(store, id)
        XCTAssertEqual(p.exp, 2_000, "부적이 경험치에 안 걸렸다")
        XCTAssertEqual(p.eggProgress, 500_000, "부적이 알까지 불렸다")
    }

    /// **두 계량기 모두 자기 상한에서 멈춘다.**
    func testBothMetersStopAtTheirOwnCeiling() {
        let store = makeStore()
        let id = partnered(store)
        store.update(todayTokens: 10_000_000_000, todayDate: "2026-08-12", hasUsageData: true)

        let p = partner(store, id)
        XCTAssertEqual(p.exp, GrowthRate.mediumFast.totalExp(at: 100))
        XCTAssertEqual(p.level, 100)
        XCTAssertEqual(p.eggProgress, ExpBalance.eggThreshold(grade: .common))
    }

    /// **알을 받으면 알 계량기만 0이 된다.** 경험치는 그대로다 — 이게 "분리"의 뜻이다.
    func testTakingTheEggResetsOnlyTheEggMeter() {
        let store = makeStore()
        let id = partnered(store)
        store.update(todayTokens: 10_000_000_000, todayDate: "2026-08-12", hasUsageData: true)
        let expBefore = partner(store, id).exp
        let line = EvoLine(baseID: 4, tree: EvoNode(speciesID: 4, children: []),
                           rarity: .common, names: [:])

        XCTAssertTrue(store.canTakeFoundEgg(partner(store, id), line: line))
        store.takeFoundEgg(individualID: id, line: line)

        let p = partner(store, id)
        XCTAssertEqual(p.eggProgress, 0, "알 계량기가 안 비었다")
        XCTAssertEqual(p.exp, expBefore, "알을 받았더니 경험치가 깎였다")
    }

    /// **사탕은 EXP 를 준다.** 값어치는 지금과 같다(1억 토큰 ÷ 500).
    func testCandyGivesExperienceNotTokens() {
        let store = makeStore()
        let id = partnered(store)
        store.mutate { $0.inventory[ShopItem.expCandy.rawValue] = 1 }

        XCTAssertTrue(store.useExpCandy(on: id))
        let p = partner(store, id)
        XCTAssertEqual(p.exp, ExpBalance.candyExp)
        XCTAssertEqual(ExpBalance.candyExp, 200_000)
        XCTAssertEqual(p.eggProgress, 0, "사탕이 알까지 채웠다")
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `swift test --filter LevelEconomyTests`
Expected: 컴파일 실패 — `type 'ExpBalance' has no member 'tokensPerExp'`

- [ ] **Step 3: 구현한다**

`PlayerStore.update` 의 파트너 블록에서 exp 적립을 둘로 나눈다:

```swift
                // 경험치와 알은 **같은 토큰 흐름에서 나란히** 찬다. 서로를 깎지 않는다.
                // 예전에는 `exp` 하나가 둘을 겸해서, 진화가 알 진행분을 먹었다.
                let expCap = state.box[index].growthRate.totalExp(at: GrowthRate.maxLevel)
                let gained = Self.expGain(delta, charm: state.ownsExpCharm) / ExpBalance.tokensPerExp
                state.box[index].exp = min(expCap, state.box[index].exp + gained)
                // 알은 토큰 그대로다 — 부적을 안 받는다(경험치 부적은 경험치에 걸린다).
                // 상한에서 멈추는 이유는 예전과 같다: 안 멈추면 안 열어 본 사이에 알이 여러 개
                // 예약돼, 받는 것이 결정이 아니라 밀린 수령이 된다.
                let eggCap = ExpBalance.eggThreshold(grade: state.box[index].grade)
                state.box[index].eggProgress = min(eggCap, state.box[index].eggProgress + delta)
```

`PlayerStore+Shop.useExpCandy` 는 `ExpBalance.candyExp` 를 더하고 상한을 건다:

```swift
            let cap = $0.box[index].growthRate.totalExp(at: GrowthRate.maxLevel)
            $0.box[index].exp = min(cap, $0.box[index].exp
                                    + PlayerStore.expGain(ExpBalance.candyExp, charm: $0.ownsExpCharm))
```

`PlayerStore+Shop.expCandyAmount` 는 삭제한다(더 이상 쓰이지 않는다).

`PlayerStore+FoundEgg` 의 판정과 초기화를 `eggProgress` 로 바꾸고 **최종진화 조건을 지운다**:

```swift
        guard individual.eggProgress >= ExpBalance.eggThreshold(grade: individual.grade) else { return false }
```
```swift
            s.box[i].eggProgress = 0
```

`FoundEggAnnouncementCard.swift:59` 도 같은 판정으로 바꾸고, 최종진화 검사를 지운다.

- [ ] **Step 4: 통과를 확인한다**

Run: `swift test --filter LevelEconomyTests` → PASS (8개)
Run: `swift build --build-tests` → 경고 0. `ExpBalance.threshold` 는 아직 살아 있으므로 컴파일이 깨지지 않는다.

- [ ] **Step 5: 커밋**

```bash
git add Sources/PokeDexBar/Player/PlayerStore.swift Sources/PokeDexBar/Player/PlayerStore+Shop.swift \
        Sources/PokeDexBar/Player/PlayerStore+FoundEgg.swift \
        Sources/PokeDexBar/UI/FoundEggAnnouncementCard.swift Tests/PokeDexBarTests/LevelEconomyTests.swift
git commit -m "feat: split experience and the Egg into two meters"
```

---

### Task 4: PokéAPI 가 주는 것을 다 읽는다

**Files:**
- Modify: `Sources/PokeDexBar/Core/PokeAPIClient.swift` (`SpeciesDTO`, `BaseSpecies`, `EvolutionDetail`, `requirement(from:)`, `node(from:)`, base-index 스냅샷 버전)
- Modify: `Sources/PokeDexBar/Core/CompanionModel.swift` (`EvoRequirementRaw`, `EvoNode.requirement`)
- Modify: `Sources/PokeDexBar/Player/EvolutionItem.swift` (`EvoRequirement` 새 case, 새 도구 3종, `walkSeconds`)
- Test: `Tests/PokeDexBarTests/EvolutionRequirementTests.swift`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `EvoRequirementRaw`: `.none`, `.item(String)`, `.friendship`, **`.level(Int)`**, **`.owns(Int)`**, **`.walked`**
  - `EvoRequirement`: 같은 다섯 + `static let walkSeconds = 6 * 3600`
  - `EvolutionItem`: `ovalStone = "oval-stone"`, `razorClaw = "razor-claw"`, `razorFang = "razor-fang"`
  - `BaseSpecies.growthRate: GrowthRate`
  - `EvoLine.growthRates: [Int: GrowthRate]` + `func growthRate(of speciesID: Int) -> GrowthRate?` — 진화 후 곡선을 갈아 끼우려면 라인이 종별 성장 타입을 실어야 한다(Task 7 이 쓴다)
  - `PokeAPIClient.requirement(from:parentLevel:) -> EvoRequirementRaw` — **Task 5 에서 `speciesID` 인자가 는다**
  - `EvoBalance.unstatedLevel = 20`, `EvoBalance.marginOverParent = 8`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```swift
import XCTest
@testable import PokeDexBar

/// PokéAPI 진화 조건 파싱 — 실제 응답 모양 그대로 만든 픽스처로 잰다.
final class EvolutionRequirementTests: XCTestCase {

    private func detail(_ json: String) throws -> EvolutionDetail {
        try JSONDecoder().decode(EvolutionDetail.self, from: Data(json.utf8))
    }

    /// `min_level` 을 읽는다 — 파이리는 16레벨.
    func testAStatedLevelIsUsedAsIs() throws {
        let d = try detail(#"{"trigger":{"name":"level-up","url":null},"min_level":16}"#)
        XCTAssertEqual(PokeAPIClient.requirement(from: [d], parentLevel: 1), .level(16))
    }

    /// **우선순위는 안 바뀐다** — 도구가 있으면 레벨보다 도구다.
    func testAnItemStillWinsOverALevel() throws {
        let d = try detail(#"""
        {"trigger":{"name":"level-up","url":null},"min_level":30,"item":{"name":"fire-stone","url":null}}
        """#)
        XCTAssertEqual(PokeAPIClient.requirement(from: [d], parentLevel: 1), .item("fire-stone"))
    }

    /// 친밀도도 레벨보다 앞선다.
    func testFriendshipStillWinsOverALevel() throws {
        let d = try detail(#"{"trigger":{"name":"level-up","url":null},"min_level":30,"min_happiness":160}"#)
        XCTAssertEqual(PokeAPIClient.requirement(from: [d], parentLevel: 1), .friendship)
    }

    /// **든 도구도 도구다.** 럭키·포푸니라·글라이온·포푸니크 넷이 여기 걸린다 —
    /// 지금까지는 통신교환일 때만 `held_item` 을 봤다. 원작의 시간대 조건은 버린다.
    func testAHeldItemOnLevelUpCountsAsAnItem() throws {
        let d = try detail(#"""
        {"trigger":{"name":"level-up","url":null},"held_item":{"name":"razor-claw","url":null},
         "time_of_day":"night"}
        """#)
        XCTAssertEqual(PokeAPIClient.requirement(from: [d], parentLevel: 1), .item("razor-claw"))
    }

    /// 통신교환은 지금 그대로 — 든 물건이 있으면 그 물건, 없으면 연결의 끈.
    func testTradeIsUnchanged() throws {
        let plain = try detail(#"{"trigger":{"name":"trade","url":null}}"#)
        XCTAssertEqual(PokeAPIClient.requirement(from: [plain], parentLevel: 1), .item("linking-cord"))
        let held = try detail(#"""
        {"trigger":{"name":"trade","url":null},"held_item":{"name":"metal-coat","url":null}}
        """#)
        XCTAssertEqual(PokeAPIClient.requirement(from: [held], parentLevel: 1), .item("metal-coat"))
    }

    /// **레벨이 안 적힌 갈래는 `max(앞 단계 + 8, 20)`.**
    /// 앞 단계가 알에서 나오면(레벨 1) 20이고, 늦게 오는 앞 단계에서는 그 위로 밀린다.
    func testAnUnstatedLevelFallsBackToTheRule() throws {
        let d = try detail(#"{"trigger":{"name":"spin","url":null}}"#)
        XCTAssertEqual(PokeAPIClient.requirement(from: [d], parentLevel: 1), .level(20))
        XCTAssertEqual(PokeAPIClient.requirement(from: [d], parentLevel: 52), .level(60))
        XCTAssertEqual(PokeAPIClient.requirement(from: [d], parentLevel: 33), .level(41))
    }

    /// 뿌리(조건 목록 자체가 없음)는 `.none` 이다.
    func testTheRootHasNoRequirement() {
        XCTAssertEqual(PokeAPIClient.requirement(from: nil, parentLevel: 1), .none)
        XCTAssertEqual(PokeAPIClient.requirement(from: [], parentLevel: 1), .none)
    }

    /// 새 도구 셋이 카탈로그에 있다 — 없으면 위 `held_item` 규칙이 조건 없음으로 샌다.
    func testTheThreeNewItemsExist() {
        XCTAssertNotNil(EvolutionItem.named("oval-stone"))
        XCTAssertNotNil(EvolutionItem.named("razor-claw"))
        XCTAssertNotNil(EvolutionItem.named("razor-fang"))
    }

    /// 성장 타입이 종 응답에서 실린다.
    func testGrowthRateComesFromTheSpeciesResponse() throws {
        let json = #"""
        {"capture_rate":45,"is_legendary":false,"is_mythical":false,"names":[],
         "evolution_chain":{"url":"https://pokeapi.co/api/v2/evolution-chain/2/"},
         "evolves_from_species":null,"growth_rate":{"name":"medium-slow","url":null}}
        """#
        let dto = try JSONDecoder().decode(SpeciesDTO.self, from: Data(json.utf8))
        XCTAssertEqual(GrowthRate.fromAPI(dto.growth_rate.name), .mediumSlow)
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `swift test --filter EvolutionRequirementTests`
Expected: 컴파일 실패 — `extra argument 'parentLevel' in call`

- [ ] **Step 3: 구현한다**

`EvolutionItem.swift` — 도구 셋을 더한다(기존 목록 옆, 같은 분류 주석 아래). `EvolutionItem` 이 이름·설명을 요구하면 다른 항목과 같은 형식으로 채운다: 둥근돌/예리한손톱/예리한이빨.

`EvoRequirement` 에 case 셋과 상수를 더한다:

```swift
    /// 이 레벨에 닿아야 한다 — 본가 `min_level`.
    case level(Int)
    /// 이 종을 박스에 갖고 있어야 한다(만타인 ← 총어). 본가의 "파티 동행" 을 옮긴 것.
    case owns(Int)
    /// 충분히 걸어야 한다 — 본가의 1000걸음. 친밀도보다 가볍다.
    case walked

    /// 걸음 진화가 요구하는 함께한 시간. 친밀도(1일)보다 짧게 잡는다 —
    /// 본가에서 1000걸음이 친밀도보다 훨씬 가벼운 것과 같은 비율이다.
    static let walkSeconds = 6 * 3600
```

`EvoRequirementRaw` 에 같은 셋을 더하고 `EvoNode.requirement` 의 매핑을 넓힌다:

```swift
        case .level(let n): .level(n)
        case .owns(let id): .owns(id)
        case .walked: .walked
```

`EvoBalance` 를 새로 만들거나(`EvolutionItem.swift` 하단) 다음 상수를 둔다:

```swift
/// 진화 레벨 규칙의 손잡이 — 본가 값이 아니라 이 앱이 정한 값이다.
enum EvoBalance {
    /// 레벨이 안 적힌 갈래의 하한. 앞 단계가 알에서 바로 나오는 25종이 여기 걸린다.
    static let unstatedLevel = 20
    /// 앞 단계 레벨 위로 얼마나 띄울지. 이게 없으면 절각참(L52)이 되는 순간
    /// 대도각참 문턱(20)을 이미 넘겨 **두 단계가 한 번에 터진다**.
    static let marginOverParent = 8
}
```

`PokeAPIClient` — `SpeciesDTO` 에 `growth_rate: NamedRef`, `BaseSpecies` 에 `growthRate: GrowthRate` 를 더하고, `EvolutionDetail` 에 `min_level: Int?` 를 더한다. base-index 스냅샷 버전을 올려 옛 캐시가 `mediumFast` 로 굳지 않게 한다.

`requirement(from:parentLevel:)` 로 바꾼다:

```swift
    static func requirement(from details: [EvolutionDetail]?, parentLevel: Int) -> EvoRequirementRaw {
        guard let details, !details.isEmpty else { return .none }
        for d in details {
            if let item = d.item?.name, EvolutionItem.named(item) != nil { return .item(item) }
            if d.trigger?.name == "trade" {
                if let held = d.held_item?.name, EvolutionItem.named(held) != nil { return .item(held) }
                return .item(EvolutionItem.linkingCord.rawValue)
            }
            // **든 도구도 도구다.** 럭키(둥근돌)·포푸니라/포푸니크(예리한손톱)·글라이온(예리한이빨)
            // 넷이 여기 걸린다 — 원작의 "밤에" 같은 시간 조건은 이 앱에 밤낮이 없어 버린다.
            if let held = d.held_item?.name, EvolutionItem.named(held) != nil { return .item(held) }
        }
        for d in details where (d.min_happiness ?? 0) > 0 { return .friendship }
        if let stated = details.compactMap(\.min_level).min() { return .level(stated) }
        return .level(max(parentLevel + EvoBalance.marginOverParent, EvoBalance.unstatedLevel))
    }
```

`node(from:)` 이 부모의 레벨을 한 칸 내려보내도록 바꾼다:

```swift
    private func node(from link: ChainLink, parentLevel: Int = 1) -> EvoNode {
        let raw = Self.requirement(from: link.evolution_details, parentLevel: parentLevel)
        let myLevel = if case .level(let n) = raw { n } else { parentLevel }
        return EvoNode(speciesID: Self.id(from: link.species.url ?? ""),
                       children: link.evolves_to.map { node(from: $0, parentLevel: myLevel) },
                       requirementRaw: raw)
    }
```

- [ ] **Step 4: 통과를 확인한다**

Run: `swift test --filter EvolutionRequirementTests` → PASS (9개)
Run: `swift build --build-tests` → 경고 0

- [ ] **Step 5: 커밋**

```bash
git add Sources/PokeDexBar/Core/PokeAPIClient.swift Sources/PokeDexBar/Core/CompanionModel.swift \
        Sources/PokeDexBar/Player/EvolutionItem.swift Tests/PokeDexBarTests/EvolutionRequirementTests.swift
git commit -m "feat: read growth rate, min level and held items from PokeAPI"
```

---

### Task 5: 레벨이 안 적힌 22종 카탈로그

**Files:**
- Create: `Sources/PokeDexBar/Player/UnstatedEvolutionCatalog.swift`
- Modify: `Sources/PokeDexBar/Core/PokeAPIClient.swift` (`requirement(from:parentLevel:)` 에 카탈로그 조회 — **종 번호가 필요하므로 `speciesID` 인자를 더한다**)
- Modify: `Tests/PokeDexBarTests/EvolutionRequirementTests.swift` (시그니처가 늘어 호출부가 전부 바뀐다)
- Test: `Tests/PokeDexBarTests/UnstatedEvolutionTests.swift`

**Interfaces:**
- Consumes: `EvoRequirementRaw`, `EvoBalance`
- Produces: `UnstatedEvolutionCatalog.override(speciesID: Int) -> EvoRequirementRaw?`
- `PokeAPIClient.requirement(from:speciesID:parentLevel:)`

**표의 근거:** 설계 문서 §3 의 A·C·D 표. 값은 PokéAPI 실측(기술 습득 레벨은 `pokemonmove` 레벨업 최신 버전그룹)에서 나왔고, 규칙은 `max(기술 습득 레벨, 앞 단계 + 8, 20)` 이다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```swift
import XCTest
@testable import PokeDexBar

/// PokéAPI 가 레벨을 안 주는 종들 — 표가 그대로 실렸나.
final class UnstatedEvolutionTests: XCTestCase {

    /// 기술을 배운 채 레벨업하는 14종. 값의 근거는 설계 문서 §3-A 표.
    func testMoveGatedSpeciesUseTheMoveLearnLevel() {
        let expected: [Int: Int] = [
            122: 25,   // 마임맨 — 흉내내기 L25
            185: 20,   // 꼬지모 — 흉내내기 L16 이지만 하한 20
            424: 32,   // 겟핸보숭 — 더블어택
            463: 34,   // 내룸벨트 — 구르기
            465: 34,   // 덩쿠림보 — 고대의힘
            469: 33,   // 메가자리 — 고대의힘
            473: 41,   // 맘모꾸리 — 고대의힘은 L1 습득이라 앞 단계(L33)+8
            763: 28,   // 달코퀸 — 밟기
            804: 20,   // 아고용 — 용의파동 L1 습득이라 하한
            853: 35,   // 케오퍼스 — 도발
            979: 36,   // 저승갓숭 — 분노의주먹 L35, 앞 단계(L28)+8
            981: 32,   // 키키링 — 트윈빔
            982: 32,   // 노고고치 — 하이퍼드릴
            1019: 20,  // 과미드라 — 드래곤치어는 레벨업 습득이 없다
        ]
        for (species, level) in expected {
            XCTAssertEqual(UnstatedEvolutionCatalog.override(speciesID: species), .level(level),
                           "#\(species)")
        }
    }

    /// 옮길 자리가 없어 레벨로 떨어뜨린 8종.
    func testUnportableConditionsBecomeLevels() {
        for species in [869, 1000, 865, 902, 899, 904, 867] {
            XCTAssertEqual(UnstatedEvolutionCatalog.override(speciesID: species), .level(20),
                           "#\(species)")
        }
        // **대도각참만 60이다.** 앞 단계인 절각참이 52레벨이라, 20으로 두면 절각참이 되는
        // 순간 이미 넘겨 두 단계가 한 번에 터진다.
        XCTAssertEqual(UnstatedEvolutionCatalog.override(speciesID: 983), .level(60))
    }

    /// 고유 규칙 5종 중 요구 조건을 갖는 넷.
    func testTheSpecialCasesGetTheirOwnRequirements() {
        XCTAssertEqual(UnstatedEvolutionCatalog.override(speciesID: 226), .owns(223))  // 만타인 ← 총어
        for species in [923, 947, 954] {                                              // 빠르모트·공푸리·베라카스
            XCTAssertEqual(UnstatedEvolutionCatalog.override(speciesID: species), .walked, "#\(species)")
        }
    }

    /// **껍질몬은 요구 조건이 아니다.** 토중몬이 진화할 때 딸려 나오는 부수 효과라
    /// 카탈로그가 아무것도 돌려주면 안 된다(돌려주면 껍질몬이 진화로도 얻어진다).
    func testShedinjaIsNotARequirement() {
        XCTAssertNil(UnstatedEvolutionCatalog.override(speciesID: 292))
    }

    /// 표에 없는 종은 건드리지 않는다 — 파이리는 PokéAPI 가 레벨을 준다.
    func testUnlistedSpeciesAreUntouched() {
        XCTAssertNil(UnstatedEvolutionCatalog.override(speciesID: 5))
        XCTAssertNil(UnstatedEvolutionCatalog.override(speciesID: 964))   // 돌핀맨 — min_level 38
    }

    /// 카탈로그는 파서가 실제로 본다 — 표만 맞고 안 불리면 아무 일도 안 난다.
    func testTheParserConsultsTheCatalog() throws {
        let d = try JSONDecoder().decode(EvolutionDetail.self,
                                         from: Data(#"{"trigger":{"name":"spin","url":null}}"#.utf8))
        // 마휘핑(#869) — 규칙대로면 앞 단계 1 → 20 이고 카탈로그도 20이라 구분이 안 된다.
        // 대도각참(#983)은 규칙(앞 단계를 1로 주면 20)과 카탈로그(60)가 갈린다.
        XCTAssertEqual(PokeAPIClient.requirement(from: [d], speciesID: 983, parentLevel: 1),
                       .level(60), "카탈로그를 안 본다")
        XCTAssertEqual(PokeAPIClient.requirement(from: [d], speciesID: 999_999, parentLevel: 1),
                       .level(20), "표에 없는 종까지 카탈로그가 가로챈다")
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `swift test --filter UnstatedEvolutionTests`
Expected: 컴파일 실패 — `cannot find 'UnstatedEvolutionCatalog' in scope`

- [ ] **Step 3: 구현한다**

```swift
import Foundation

/// PokéAPI 가 **진화 레벨을 안 주는 31종**을 이 앱의 조건으로 옮긴 표.
///
/// 왜 표인가: 원작 조건이 기술·전투·조작에 묶여 있어 런타임에 알아낼 방법이 없다. `EvolutionItem`·
/// `RegionalFormCatalog`·`StarterCatalog` 와 같은 번들 카탈로그 패턴이다.
///
/// 값의 규칙은 `max(기술 습득 레벨, 앞 단계 도달 레벨 + 8, 20)` 이고, 습득 레벨은 PokéAPI
/// `pokemonmove`(레벨업, 최신 버전그룹) 실측이다. 근거와 전수 조사는 설계 문서 §3 에 있다.
///
/// **넷은 여기 없다** — 럭키·포푸니라·포푸니크·글라이온은 든 도구로 판정되므로(`held_item`)
/// 파서가 먼저 잡는다. **껍질몬도 없다** — 요구 조건이 아니라 토중몬 진화의 부수 효과다.
enum UnstatedEvolutionCatalog {

    static func override(speciesID: Int) -> EvoRequirementRaw? { table[speciesID] }

    private static let table: [Int: EvoRequirementRaw] = [
        // ── 기술을 배운 채 레벨업 (습득 레벨) ────────────────────────────────
        122: .level(25),    // 마임맨 ← 흉내내 · 흉내내기 L25
        185: .level(20),    // 꼬지모 ← 꼬지지 · 흉내내기 L16 → 하한 20
        424: .level(32),    // 겟핸보숭 ← 에이팜 · 더블어택 L32
        463: .level(34),    // 내룸벨트 ← 내루미 · 구르기 L34
        465: .level(34),    // 덩쿠림보 ← 덩쿠리 · 고대의힘 L34
        469: .level(33),    // 메가자리 ← 왕자리 · 고대의힘 L33
        473: .level(41),    // 맘모꾸리 ← 메꾸리 · 고대의힘은 L1 습득 → 앞 단계 L33 + 8
        763: .level(28),    // 달코퀸 ← 달무리나 · 밟기 L28
        804: .level(20),    // 아고용 ← 베베놈 · 용의파동 L1 습득 → 하한 20
        853: .level(35),    // 케오퍼스 ← 때때무노 · 도발 L35
        979: .level(36),    // 저승갓숭 ← 성원숭 · 분노의주먹 L35, 앞 단계 L28 + 8
        981: .level(32),    // 키키링 ← 키링키 · 트윈빔 L32
        982: .level(32),    // 노고고치 ← 노고치 · 하이퍼드릴 L32
        1019: .level(20),   // 과미드라 ← 과미르 · 드래곤치어는 레벨업 습득이 없다 → 하한 20

        // ── 원작 조건을 옮길 자리가 없어 레벨로 ──────────────────────────────
        865: .level(20),    // 창파나이트 — 급소 3회
        867: .level(20),    // 데스판 — 피해 49 + 다리 밑
        869: .level(20),    // 마휘핑 — 회전
        899: .level(20),    // 신비록 — 스타일 기술 20회
        902: .level(20),    // 대쓰여너 — 반동 데미지
        904: .level(20),    // 장침바루 — 스타일 기술 20회
        983: .level(60),    // 대도각참 — 절각참 3마리 격파. 앞 단계 L52 + 8
        1000: .level(20),   // 타부자고 — 코인 999개

        // ── 고유 규칙 ────────────────────────────────────────────────────
        226: .owns(223),    // 만타인 — 원작은 파티에 총어 동행 → 박스에 총어(#223) 보유
        923: .walked,       // 빠르모트 — 1000걸음
        947: .walked,       // 공푸리 — 1000걸음
        954: .walked,       // 베라카스 — 1000걸음
    ]
}
```

`requirement` 에 카탈로그 조회를 끼운다 — **레벨 폴백 직전, 그러나 도구·친밀도 뒤**다. 도구가 우선인 이유는 럭키 넷이 카탈로그에 없기 때문이고, 카탈로그가 `min_level` 보다 앞서는 이유는 이 22종에 `min_level` 이 아예 없기 때문이다(있으면 그게 맞다).

```swift
        for d in details where (d.min_happiness ?? 0) > 0 { return .friendship }
        if let stated = details.compactMap(\.min_level).min() { return .level(stated) }
        if let override = UnstatedEvolutionCatalog.override(speciesID: speciesID) { return override }
        return .level(max(parentLevel + EvoBalance.marginOverParent, EvoBalance.unstatedLevel))
```

`node(from:)` 은 이미 종 번호를 알고 있으므로 그대로 넘긴다.

- [ ] **Step 4: 통과를 확인한다**

Run: `swift test --filter UnstatedEvolutionTests` → PASS (6개)
Run: `swift test --filter EvolutionRequirementTests` → 여전히 PASS (시그니처가 늘었으므로 호출부 수정 필요)

- [ ] **Step 5: 커밋**

```bash
git add Sources/PokeDexBar/Player/UnstatedEvolutionCatalog.swift Sources/PokeDexBar/Core/PokeAPIClient.swift \
        Tests/PokeDexBarTests/UnstatedEvolutionTests.swift Tests/PokeDexBarTests/EvolutionRequirementTests.swift
git commit -m "feat: catalogue every evolution PokeAPI leaves without a level"
```

---

### Task 6: 새로 생기는 개체가 자기 성장 타입을 갖는다

**Files:**
- Modify: `Sources/PokeDexBar/Player/Egg.swift` (`growthRate` 필드 — 부화 때 개체에 실어 준다)
- Modify: `Sources/PokeDexBar/Player/PlayerStore+Eggs.swift` (`placeEgg`/`startEgg` 가 성장 타입을 받아 알에 적는다)
- Modify: `Sources/PokeDexBar/Player/PlayerStore+Hatching.swift:75` (알의 값을 개체로)
- Modify: `Sources/PokeDexBar/Player/PlayerStore.swift:56` (`chooseStarter`)
- Modify: `Sources/PokeDexBar/Player/PlayerStore+Professor.swift` (`offeredIndividual` — `index` 에 `BaseSpecies` 가 있다)
- Modify: `Sources/PokeDexBar/UI/ShopTabView.swift` (뽑기 착지에서 고른 `BaseSpecies` 의 값을 넘긴다)
- Test: `Tests/PokeDexBarTests/GrowthRateWiringTests.swift`

**Interfaces:**
- Consumes: `BaseSpecies.growthRate`, `Egg.growthRate`, `Individual.growthRate`
- Produces:
  - `Egg.growthRate: GrowthRate` (기본 `.mediumFast`, 관대 디코딩)
  - `placeEgg(grade:speciesID:shiny:growthRate:)` / `startEgg(grade:speciesID:shiny:growthRate:)` — 기존 호출부가 안 깨지게 **기본값 `.mediumFast`** 를 둔다

**왜 알에 싣나:** 부화 시점에는 `BaseSpecies` 가 없다. 뽑기·박사 경로에는 있으므로 그때 알에
적어 두면 부화가 네트워크 없이 그대로 물려줄 수 있다 — `shiny`·`grade` 를 알이 들고 있는 것과
같은 이유다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```swift
import XCTest
@testable import PokeDexBar

/// 새로 만들어지는 개체가 자기 곡선을 갖고 태어나나.
@MainActor
final class GrowthRateWiringTests: XCTestCase {
    private let now = Date(timeIntervalSince1970: 1_700_000_000)

    private func makeStore() -> PlayerStore {
        PlayerStore(fileURL: FileManager.default.temporaryDirectory
                        .appendingPathComponent("gr-\(UUID().uuidString).json"),
                    rng: SeededRNG(seed: 1), now: { self.now })
    }

    /// **알이 성장 타입을 들고 있다가 부화한 개체에 넘긴다.**
    func testAHatchedIndividualInheritsTheEggsGrowthRate() {
        let store = makeStore()
        store.seedForTesting(wallet: 0, slots: 3, eggs: 0, at: now)
        let egg = store.placeEgg(grade: .common, speciesID: 4, shiny: false, growthRate: .mediumSlow)
        XCTAssertEqual(egg?.growthRate, .mediumSlow, "알이 성장 타입을 안 적었다")

        store.mutate { $0.eggs[0].hatchesAt = self.now.addingTimeInterval(-1) }
        store.hatch(baseID: 4)

        XCTAssertEqual(store.state.box.last?.growthRate, .mediumSlow, "부화한 개체가 물려받지 못했다")
    }

    /// **대조군** — 다른 값이면 다르게 실린다. 하나만 검사하면 하드코딩도 통과한다.
    func testADifferentGrowthRateArrivesDifferently() {
        let store = makeStore()
        store.seedForTesting(wallet: 0, slots: 3, eggs: 0, at: now)
        _ = store.placeEgg(grade: .common, speciesID: 4, shiny: false, growthRate: .fluctuating)
        store.mutate { $0.eggs[0].hatchesAt = self.now.addingTimeInterval(-1) }
        store.hatch(baseID: 4)

        XCTAssertEqual(store.state.box.last?.growthRate, .fluctuating)
    }

    /// **박사의 제안도 후보의 성장 타입을 싣는다.**
    func testAProfessorOfferCarriesTheGrowthRate() {
        let store = makeStore()
        store.update(todayTokens: 0, todayDate: "2026-08-12", hasUsageData: true)
        let index = [BaseSpecies(id: 4, captureRate: 45, isLegendary: false, isMythical: false,
                                 growthRate: .mediumSlow)]
        store.refreshProfessorOffers(index: index)

        XCTAssertEqual(store.state.professorOffers.first?.individual.growthRate, .mediumSlow)
    }

    /// 구 세이브의 알에는 필드가 없다 — 없다고 알을 버리면 부화 중인 것이 다 날아간다.
    func testAnOldEggStillDecodes() throws {
        let json = """
        {"id":"\(UUID().uuidString)","grade":"common","speciesID":4,"shiny":false,
         "startedAt":0,"hatchesAt":100}
        """
        let egg = try JSONDecoder().decode(Egg.self, from: Data(json.utf8))
        XCTAssertEqual(egg.speciesID, 4)
        XCTAssertEqual(egg.growthRate, .mediumFast)
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `swift test --filter GrowthRateWiringTests`
Expected: 컴파일 실패 — `extra argument 'growthRate' in call`

- [ ] **Step 3: 구현한다**

`Egg` 에 필드를 더한다(관대 디코딩 — 파일 상단 주석의 이유 그대로):

```swift
    /// 이 알에서 나올 개체의 경험치 곡선. 부화 시점에는 `BaseSpecies` 가 없으므로
    /// 뽑을 때 적어 둔다 — `shiny`·`grade` 를 알이 들고 있는 것과 같은 이유다.
    var growthRate: GrowthRate = .mediumFast
```
`init(from:)` 에 `growthRate = value(.growthRate, .mediumFast)`, `CodingKeys` 에 항목 추가.

`placeEgg`/`startEgg` 에 `growthRate: GrowthRate = .mediumFast` 인자를 더해 알에 적고,
`PlayerStore+Hatching.swift:75` 의 개체 생성에 `growthRate: egg.growthRate` 를 넘긴다.

`chooseStarter` 는 스타터 종의 값을 모르므로(카탈로그만 있다) `.mediumFast` 기본값을 그대로 쓴다 —
첫 라인을 받는 순간 Task 7 의 진화 갱신이 맞춰 준다. 이 사실을 주석으로 남긴다.

`offeredIndividual` 은 `index` 에서 고른 `BaseSpecies` 의 `growthRate` 를 개체에 싣는다.

`ShopTabView.landDraw` 는 고른 종의 `BaseSpecies` 를 이미 손에 들고 있으므로 그 값을 넘긴다.

- [ ] **Step 4: 통과를 확인한다**

Run: `swift test --filter GrowthRateWiringTests` → PASS (4개)
Run: `swift build --build-tests` → 경고 0

- [ ] **Step 5: 커밋**

```bash
git add Sources/PokeDexBar/Player/Egg.swift Sources/PokeDexBar/Player/PlayerStore+Eggs.swift \
        Sources/PokeDexBar/Player/PlayerStore+Hatching.swift Sources/PokeDexBar/Player/PlayerStore.swift \
        Sources/PokeDexBar/Player/PlayerStore+Professor.swift Sources/PokeDexBar/UI/ShopTabView.swift \
        Tests/PokeDexBarTests/GrowthRateWiringTests.swift
git commit -m "feat: carry each species growth rate from the draw to the hatchling"
```

---

### Task 7: 진화 게이트를 레벨로 바꾼다

**Files:**
- Modify: `Sources/PokeDexBar/Player/PlayerStore+Evolution.swift`
- Test: `Tests/PokeDexBarTests/LevelEvolutionTests.swift`

**Interfaces:**
- Consumes: `EvoRequirement.level/.owns/.walked/.item/.friendship`, `Individual.level`, `EvoRequirement.walkSeconds`
- Produces:
  - `func meetsRequirement(_ requirement: EvoRequirement, for individual: Individual) -> Bool` (시그니처 유지 — `.owns` 는 `state.box` 를 보므로 인스턴스 메서드인 채로 충분하다)
  - `func canEvolve(_ individual: Individual, to speciesID: Int, line: EvoLine) -> Bool`
  - `evolve(individualID:to:line:)` — 경험치를 깎지 않고, 성장 타입을 갱신하고, 토중몬이면 껍질몬을 더한다

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```swift
import XCTest
@testable import PokeDexBar

/// 진화가 레벨로 열린다.
@MainActor
final class LevelEvolutionTests: XCTestCase {
    private let now = Date(timeIntervalSince1970: 1_700_000_000)

    private func makeStore() -> PlayerStore {
        PlayerStore(fileURL: FileManager.default.temporaryDirectory
                        .appendingPathComponent("evo-\(UUID().uuidString).json"),
                    rng: SeededRNG(seed: 1), now: { self.now })
    }

    /// 파이리 → 리자드(L16) 한 갈래짜리 라인.
    private func charmanderLine() -> EvoLine {
        EvoLine(baseID: 4,
                tree: EvoNode(speciesID: 4,
                              children: [EvoNode(speciesID: 5, children: [],
                                                 requirementRaw: .level(16))]),
                rarity: .common, names: [:])
    }

    private func add(_ store: PlayerStore, exp: Int, rate: GrowthRate = .mediumSlow) -> Individual {
        let individual = Individual(baseID: 4, speciesID: 4, pathIDs: [4], nature: .hardy,
                                    exp: exp, obtainedAt: now, grade: .common, growthRate: rate)
        store.addForTesting(individual)
        return individual
    }

    /// **레벨이 모자라면 못 한다.** L15 에서는 아직이다.
    func testBelowTheLevelItCannotEvolve() {
        let store = makeStore()
        let individual = add(store, exp: GrowthRate.mediumSlow.totalExp(at: 15))
        XCTAssertEqual(individual.level, 15)
        XCTAssertFalse(store.canEvolve(individual, to: 5, line: charmanderLine()))
        XCTAssertFalse(store.evolve(individualID: individual.id, to: 5, line: charmanderLine()))
        XCTAssertEqual(store.state.box.first?.speciesID, 4)
    }

    /// **레벨을 채우면 된다.** 대조군이 없으면 "항상 false" 구현도 위 테스트를 통과한다.
    func testAtTheLevelItEvolves() {
        let store = makeStore()
        let individual = add(store, exp: GrowthRate.mediumSlow.totalExp(at: 16))
        XCTAssertEqual(individual.level, 16)
        XCTAssertTrue(store.canEvolve(individual, to: 5, line: charmanderLine()))
        XCTAssertTrue(store.evolve(individualID: individual.id, to: 5, line: charmanderLine()))
        XCTAssertEqual(store.state.box.first?.speciesID, 5)
    }

    /// **진화해도 경험치는 안 깎인다.** 본가에서 진화는 레벨을 되돌리지 않는다.
    func testEvolvingKeepsTheExperience() {
        let store = makeStore()
        let exp = GrowthRate.mediumSlow.totalExp(at: 30)
        let individual = add(store, exp: exp)
        store.evolve(individualID: individual.id, to: 5, line: charmanderLine())

        XCTAssertEqual(store.state.box.first?.exp, exp, "진화가 경험치를 깎았다")
        XCTAssertEqual(store.state.box.first?.level, 30)
    }

    /// **진화해도 알 진행분은 안 깎인다.** 두 계량기가 서로를 안 건드린다는 주장의 반쪽이다.
    func testEvolvingKeepsTheEggProgress() {
        let store = makeStore()
        var individual = add(store, exp: GrowthRate.mediumSlow.totalExp(at: 20))
        individual.eggProgress = 123_456_789
        store.mutate { $0.box = [individual] }
        store.evolve(individualID: individual.id, to: 5, line: charmanderLine())

        XCTAssertEqual(store.state.box.first?.eggProgress, 123_456_789)
    }

    /// **도구 진화는 레벨을 안 본다** — 본가가 그렇다. 갓 부화한 개체도 돌만 있으면 된다.
    func testAnItemEvolutionIgnoresTheLevel() {
        let store = makeStore()
        let line = EvoLine(baseID: 4,
                           tree: EvoNode(speciesID: 4,
                                         children: [EvoNode(speciesID: 5, children: [],
                                                            requirementRaw: .item("fire-stone"))]),
                           rarity: .common, names: [:])
        let individual = add(store, exp: 0)
        XCTAssertEqual(individual.level, 1)
        XCTAssertFalse(store.canEvolve(individual, to: 5, line: line), "도구가 없는데 된다")

        store.mutate { $0.inventory["fire-stone"] = 1 }
        XCTAssertTrue(store.canEvolve(store.state.box.first!, to: 5, line: line))
    }

    /// **`.owns` — 만타인은 박스에 총어가 있어야 한다.**
    func testTheOwnsRequirementLooksAtTheBox() {
        let store = makeStore()
        let line = EvoLine(baseID: 458,
                           tree: EvoNode(speciesID: 458,
                                         children: [EvoNode(speciesID: 226, children: [],
                                                            requirementRaw: .owns(223))]),
                           rarity: .common, names: [:])
        let mantyke = Individual(baseID: 458, speciesID: 458, pathIDs: [458], nature: .hardy,
                                 obtainedAt: now, grade: .common)
        store.addForTesting(mantyke)
        XCTAssertFalse(store.canEvolve(mantyke, to: 226, line: line), "총어가 없는데 된다")

        store.addForTesting(Individual(baseID: 223, speciesID: 223, pathIDs: [223], nature: .hardy,
                                       obtainedAt: now, grade: .common))
        XCTAssertTrue(store.canEvolve(store.state.box.first!, to: 226, line: line))
    }

    /// **`.walked` — 함께한 시간 6시간.**
    func testTheWalkedRequirementUsesPartnerTime() {
        let store = makeStore()
        let line = EvoLine(baseID: 921,
                           tree: EvoNode(speciesID: 921,
                                         children: [EvoNode(speciesID: 923, children: [],
                                                            requirementRaw: .walked)]),
                           rarity: .common, names: [:])
        var pawmi = Individual(baseID: 921, speciesID: 921, pathIDs: [921], nature: .hardy,
                               obtainedAt: now, grade: .common)
        pawmi.partnerSeconds = EvoRequirement.walkSeconds - 1
        store.mutate { $0.box = [pawmi] }
        XCTAssertFalse(store.canEvolve(pawmi, to: 923, line: line), "1초 모자란데 된다")

        pawmi.partnerSeconds = EvoRequirement.walkSeconds
        store.mutate { $0.box = [pawmi] }
        XCTAssertTrue(store.canEvolve(pawmi, to: 923, line: line))
    }

    /// **토중몬이 아이스크가 되면 껍질몬이 박스에 하나 더 생긴다.**
    func testEvolvingNincadaAlsoLeavesAShedinja() {
        let store = makeStore()
        let line = EvoLine(baseID: 290,
                           tree: EvoNode(speciesID: 290,
                                         children: [EvoNode(speciesID: 291, children: [],
                                                            requirementRaw: .level(20))]),
                           rarity: .common, names: [:])
        var nincada = Individual(baseID: 290, speciesID: 290, pathIDs: [290], nature: .hardy,
                                 exp: GrowthRate.erratic.totalExp(at: 20), obtainedAt: now,
                                 grade: .common, growthRate: .erratic)
        nincada.id = UUID()
        store.mutate { $0.box = [nincada] }

        XCTAssertTrue(store.evolve(individualID: nincada.id, to: 291, line: line))
        XCTAssertEqual(store.state.box.count, 2, "껍질몬이 안 생겼다")
        XCTAssertTrue(store.state.box.contains { $0.speciesID == 292 })
        XCTAssertTrue(store.state.dex.contains(292), "도감에 껍질몬이 안 들어갔다")
    }

    /// 껍질몬은 **토중몬일 때만** 생긴다 — 아무 진화에나 딸려 나오면 안 된다.
    func testOtherEvolutionsDoNotLeaveAShedinja() {
        let store = makeStore()
        let individual = add(store, exp: GrowthRate.mediumSlow.totalExp(at: 16))
        store.evolve(individualID: individual.id, to: 5, line: charmanderLine())
        XCTAssertEqual(store.state.box.count, 1)
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `swift test --filter LevelEvolutionTests`
Expected: 컴파일 실패 — `extra argument 'to' in call`

- [ ] **Step 3: 구현한다**

```swift
    /// 그 조건을 지금 만족하는가. 도구는 **갖고 있으면** 되고(쓰는 건 진화 실행 때),
    /// 친밀도·걸음은 그 개체와 함께한 시간으로, 동행은 박스에 그 종이 있는지로 판단한다.
    func meetsRequirement(_ requirement: EvoRequirement, for individual: Individual) -> Bool {
        switch requirement {
        case .none: true
        case .item(let item): count(of: item) > 0
        case .level(let need): individual.level >= need
        case .owns(let speciesID): state.box.contains { $0.speciesID == speciesID }
        case .friendship:
            individual.partnerDuration(at: currentDate()) >= EvoRequirement.friendshipSeconds
        case .walked:
            individual.partnerDuration(at: currentDate()) >= EvoRequirement.walkSeconds
        }
    }

    /// 이 종으로 갈 수 있고, 그 조건을 만족하는가. **경험치 임계는 더 이상 없다** —
    /// 조건 자체가 게이트다(레벨 진화면 레벨이, 도구 진화면 도구가).
    func canEvolve(_ individual: Individual, to speciesID: Int, line: EvoLine) -> Bool {
        evolutionChoices(individual, line: line).contains(speciesID)
            && meetsRequirement(requirement(for: speciesID, line: line), for: individual)
    }
```

`evolve` 는 게이트를 `canEvolve` 하나로 줄이고, **경험치 차감을 지우고**, 성장 타입 갱신과 껍질몬을 더한다:

```swift
        guard canEvolve(individual, to: speciesID, line: line) else { return false }
        let hadSpeedup = HatchSpeedup.present(in: state.box)
        mutate { state in
            state.box[index].speciesID = speciesID
            state.box[index].pathIDs.append(speciesID)
            // **경험치는 그대로 둔다.** 본가에서 진화는 레벨을 되돌리지 않는다.
            // 알 진행분도 마찬가지다 — 두 계량기 모두 진화와 무관하다.
            state.box[index].form = nil
            // 성장 타입은 종에 달린 것이라 진화하면 새 종의 것으로 갱신한다.
            if let rate = line.growthRate(of: speciesID) { state.box[index].growthRate = rate }
            state.dex.insert(speciesID)
            // 토중몬 → 아이스크. 본가에서 빈 몬스터볼에 껍질몬이 남는 것을 옮긴 것 —
            // 요구 조건이 아니라 **부수 효과**라 여기에 있다.
            if Self.shedinjaParent == individual.speciesID, speciesID == Self.shedinjaSibling {
                var shed = state.box[index]
                shed.id = UUID()
                shed.speciesID = Self.shedinjaID
                shed.pathIDs = individual.pathIDs + [Self.shedinjaID]
                shed.eggProgress = 0
                state.box.append(shed)
                state.dex.insert(Self.shedinjaID)
            }
        }
```

상수를 같은 파일 상단에 둔다:

```swift
    /// 토중몬(#290) → 아이스크(#291) 진화에 껍질몬(#292)이 딸려 나온다.
    static let shedinjaParent = 290
    static let shedinjaSibling = 291
    static let shedinjaID = 292
```

`line.growthRate(of:)` 는 Task 4 가 만들어 둔 것이다.

- [ ] **Step 4: 통과를 확인한다**

Run: `swift test --filter LevelEvolutionTests` → PASS (9개)
Run: `swift build --build-tests` → 경고 0

- [ ] **Step 5: 커밋**

```bash
git add Sources/PokeDexBar/Player/PlayerStore+Evolution.swift Sources/PokeDexBar/Core/CompanionModel.swift \
        Tests/PokeDexBarTests/LevelEvolutionTests.swift
git commit -m "feat: gate evolution on level, party and steps instead of experience"
```

---

### Task 8: 박사에게 보내는 값을 레벨로

**Files:**
- Modify: `Sources/PokeDexBar/Player/ReleaseBalance.swift`
- Modify: `Tests/PokeDexBarTests/ProfessorTests.swift` (경험치 비율을 쓰던 두 테스트)
- Test: 같은 파일에 추가

**Interfaces:**
- Consumes: `Individual.level`
- Produces: `ReleaseBalance.points(for:) -> Int` (레벨 기반)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`ProfessorTests` 에 더한다:

```swift
    /// **값은 레벨을 따른다.** 100레벨이 한 단계를 통째로 더 얹는 것과 같다.
    func testReleasePointsFollowTheLevel() {
        func made(_ level: Int) -> Individual {
            Individual(baseID: 4, speciesID: 4, pathIDs: [4], nature: .hardy,
                       exp: GrowthRate.mediumFast.totalExp(at: level), obtainedAt: now,
                       grade: .epic, growthRate: .mediumFast)
        }
        XCTAssertEqual(ReleaseBalance.points(for: made(1)), 12)    // 12 × (0 + 1 + 0.01)
        XCTAssertEqual(ReleaseBalance.points(for: made(50)), 18)   // 12 × (0 + 1 + 0.50)
        XCTAssertEqual(ReleaseBalance.points(for: made(100)), 24)  // 12 × (0 + 1 + 1.00)
    }

    /// 진화 단계도 그대로 값에 실린다 — 레벨만 보는 구현이면 여기서 걸린다.
    func testReleasePointsStillCountTheStages() {
        let grown = Individual(baseID: 4, speciesID: 6, pathIDs: [4, 5, 6], nature: .hardy,
                               exp: GrowthRate.mediumFast.totalExp(at: 100), obtainedAt: now,
                               grade: .epic, growthRate: .mediumFast)
        XCTAssertEqual(ReleaseBalance.points(for: grown), 48)      // 12 × (2 + 1 + 1.00)
    }
```

기존 `testReleasePointsIncludeBankedExperience` 와 `testReleasePointsClampTheExperienceRatio` 는 경험치 비율 기준이라 위 둘로 **대체한다**(지운다).

- [ ] **Step 2: 실패를 확인한다**

Run: `swift test --filter ProfessorTests`
Expected: FAIL — 값이 안 맞는다

- [ ] **Step 3: 구현한다**

**여기서 `ExpBalance.threshold(grade:stageIndex:)` 를 지운다** — Task 7 이 진화에서 뺐고 이곳이
마지막 사용처다. 남겨 두면 아무도 안 쓰는 임계표가 밸런스 문서처럼 계속 읽힌다.

```swift
    /// `등급기본 × (진화 횟수 + 1 + 레벨/100)`, 내림.
    ///
    /// 예전에는 "지금 단계에서 채운 경험치 비율" 이었는데, 경험치가 레벨이 되면서 **레벨 자체가
    /// 얼마나 키웠나** 를 곧바로 말해 준다. 100레벨이 한 단계를 통째로 더 얹는 것과 같아
    /// 자릿수는 예전과 같다.
    static func points(for individual: Individual) -> Int {
        let grown = Double(individual.stageIndex) + 1
            + Double(individual.level) / Double(GrowthRate.maxLevel)
        return Int(Double(base(grade: individual.grade)) * grown)
    }
```

- [ ] **Step 4: 통과를 확인한다**

Run: `swift test --filter ProfessorTests` → PASS

- [ ] **Step 5: 커밋**

```bash
git add Sources/PokeDexBar/Player/ReleaseBalance.swift Tests/PokeDexBarTests/ProfessorTests.swift
git commit -m "feat: price a Pokemon by its level when sending it to the Professor"
```

---

### Task 9: 화면

**Files:**
- Modify: `Sources/PokeDexBar/UI/IndividualDetailView.swift` (경험치 줄 → 레벨 + 알 바)
- Modify: `Sources/PokeDexBar/UI/BoxTabView.swift` (칸에 `Lv.` 배지)
- Modify: `Sources/PokeDexBar/Core/Localization.swift` (문구 ko/en/ja)
- Test: `Tests/PokeDexBarTests/LevelViewTests.swift`

**Interfaces:**
- Consumes: `Individual.level`, `Individual.exp`, `Individual.eggProgress`, `GrowthRate.totalExp(at:)`
- Produces:
  - `L.levelLabel(_ level: Int) -> String`
  - `L.expToNextLevel(_ remaining: String) -> String`
  - `L.eggProgressLabel: String`
  - `IndividualDetailView.levelProgress(_ individual: Individual) -> Double` (0…1, 순수)
  - `IndividualDetailView.expToNext(_ individual: Individual) -> Int` (순수)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```swift
import XCTest
import SwiftUI
@testable import PokeDexBar

@MainActor
final class LevelViewTests: XCTestCase {
    private let now = Date(timeIntervalSince1970: 1_700_000_000)

    private func made(_ level: Int, plus: Int = 0) -> Individual {
        Individual(baseID: 4, speciesID: 4, pathIDs: [4], nature: .hardy,
                   exp: GrowthRate.mediumFast.totalExp(at: level) + plus,
                   obtainedAt: now, grade: .common, growthRate: .mediumFast)
    }

    /// 다음 레벨까지 남은 EXP — 딱 도달한 순간에는 그 레벨 구간 전체가 남는다.
    func testExpToNextLevel() {
        let l50 = made(50)
        let span = GrowthRate.mediumFast.totalExp(at: 51) - GrowthRate.mediumFast.totalExp(at: 50)
        XCTAssertEqual(IndividualDetailView.expToNext(l50), span)
        XCTAssertEqual(IndividualDetailView.expToNext(made(50, plus: span - 1)), 1)
    }

    /// 100레벨은 다음이 없다 — 남은 값이 0이고 진행도가 가득이다.
    func testMaxLevelHasNothingLeft() {
        XCTAssertEqual(IndividualDetailView.expToNext(made(100)), 0)
        XCTAssertEqual(IndividualDetailView.levelProgress(made(100)), 1, accuracy: 1e-9)
    }

    /// 진행도는 그 레벨 구간 안의 비율이다 — 레벨이 오른 직후엔 0에 가깝다.
    func testProgressIsWithinTheCurrentLevel() {
        XCTAssertEqual(IndividualDetailView.levelProgress(made(50)), 0, accuracy: 1e-9)
        let span = GrowthRate.mediumFast.totalExp(at: 51) - GrowthRate.mediumFast.totalExp(at: 50)
        XCTAssertEqual(IndividualDetailView.levelProgress(made(50, plus: span / 2)),
                       0.5, accuracy: 0.01)
    }

    /// 문구가 세 언어를 다 채운다.
    func testLevelStringsCoverAllThreeLanguages() {
        for lang in AppLanguage.allCases {
            XCTAssertFalse(L(lang).levelLabel(47).isEmpty, "\(lang)")
            XCTAssertFalse(L(lang).expToNextLevel("8,240").isEmpty, "\(lang)")
            XCTAssertFalse(L(lang).eggProgressLabel.isEmpty, "\(lang)")
        }
        XCTAssertTrue(L(.en).levelLabel(47).contains("47"))
    }

    /// **화면이 실제로 레벨을 그린다.** 순수 함수가 다 맞아도 뷰가 안 쓰면 아무 일도 안 난다.
    func testTheDetailViewRendersTheLevel() throws {
        let root = URL(fileURLWithPath: #filePath)
            .deletingLastPathComponent().deletingLastPathComponent().deletingLastPathComponent()
        let source = try String(contentsOf: root.appendingPathComponent(
            "Sources/PokeDexBar/UI/IndividualDetailView.swift"), encoding: .utf8)
        XCTAssertTrue(source.contains("levelLabel"), "상세에 레벨 문구가 없다")
        XCTAssertTrue(source.contains("eggProgress"), "상세에 알 진행이 없다")
        XCTAssertFalse(source.contains("ExpBalance.threshold("), "옛 진화 임계가 남아 있다")
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `swift test --filter LevelViewTests`
Expected: 컴파일 실패 — `type 'IndividualDetailView' has no member 'expToNext'`

- [ ] **Step 3: 구현한다**

`Localization.swift` 에 문구를 더한다:

```swift
    func levelLabel(_ level: Int) -> String { t("Lv.\(level)", "Lv.\(level)", "Lv.\(level)") }
    func expToNextLevel(_ remaining: String) -> String {
        t("다음 레벨까지 \(remaining)", "\(remaining) to next level", "つぎのレベルまで \(remaining)")
    }
    var eggProgressLabel: String { t("알", "Egg", "タマゴ") }
```

`IndividualDetailView` 에 순수 함수 둘을 더하고, 경험치 줄을 레벨 + 알 바로 바꾼다:

```swift
    /// 다음 레벨까지 남은 EXP. 100레벨이면 0.
    nonisolated static func expToNext(_ individual: Individual) -> Int {
        let level = individual.level
        guard level < GrowthRate.maxLevel else { return 0 }
        return individual.growthRate.totalExp(at: level + 1) - individual.exp
    }

    /// 지금 레벨 구간 안에서의 진행도(0…1). 100레벨이면 가득.
    nonisolated static func levelProgress(_ individual: Individual) -> Double {
        let level = individual.level
        guard level < GrowthRate.maxLevel else { return 1 }
        let floorExp = individual.growthRate.totalExp(at: level)
        let span = individual.growthRate.totalExp(at: level + 1) - floorExp
        guard span > 0 else { return 1 }
        return min(1, max(0, Double(individual.exp - floorExp) / Double(span)))
    }
```

`BoxTabView` 의 칸 이름 옆에 `store.l.levelLabel(individual.level)` 을 작은 글씨로 붙인다.

- [ ] **Step 4: 통과를 확인한다**

Run: `swift test --filter LevelViewTests` → PASS (5개)
Run: `swift test` → 전체 PASS, 경고 0

- [ ] **Step 5: 렌더해서 눈으로 확인한다**

`Tests/PokeDexBarTests/` 에 임시 렌더 테스트를 만들어 상세 화면과 박스를 PNG 로 뽑아 확인하고, **확인 후 그 파일을 지운다**. 레벨 배지가 이름을 밀어내지 않는지, 알 바가 파트너일 때만 뜨는지를 본다.

- [ ] **Step 6: 커밋**

```bash
git add Sources/PokeDexBar/UI/IndividualDetailView.swift Sources/PokeDexBar/UI/BoxTabView.swift \
        Sources/PokeDexBar/Core/Localization.swift Tests/PokeDexBarTests/LevelViewTests.swift
git commit -m "feat: show levels instead of raw token counts"
```

---

## 마무리 (모든 태스크 후)

- [ ] `swift test` 전체 통과, `swift build --build-tests` 경고 0.
- [ ] **뮤테이션 점검** — 다음 셋을 각각 되돌려 보고 해당 테스트가 실패하는지 확인한다:
  1. `update` 에서 `/ ExpBalance.tokensPerExp` 를 지운다 → `testTokensBecomeExperienceAtFiveHundredToOne` 실패
  2. `eggProgress` 적립에 `expGain(charm:)` 을 태운다 → `testTheExpCharmDoublesExperienceButNotTheEgg` 실패
  3. 카탈로그 조회를 지운다 → `testTheParserConsultsTheCatalog` 실패
- [ ] `docs/superpowers/specs/2026-08-12-levels-design.md` 의 **상태** 줄을 "구현 완료" 로 바꾼다.
- [ ] 릴리스는 별도 — UI 를 건드리는 `feat:` 이므로 `assets/` 에 **신규** 파일이 필요하다(§릴리스 1 하드 게이트). 레벨이 보이는 상세 화면 배너를 새로 만든다.
