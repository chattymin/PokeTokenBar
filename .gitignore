# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# App bundle build output
build/

# OMC / agent local state
.omc/

# macOS
.DS_Store
*.xcuserstate

# Signing material — never commit private keys / certs
*.p12
*.pem
*.key
*.cer
*.certSigningRequest

# Personal dev notes (kept locally, not published)
docs/*
# 이 파일은 릴리스 게이트가 읽는다 — 저장소에 없으면 다른 클론에서 게이트가 못 통과한다.
!docs/undocumented.md
# This fork's design and plan documents are tracked — they are the contract
# implementation works from, not personal notes.
!docs/superpowers/

# Agent worktrees / local state
.claude/
.worktrees/
